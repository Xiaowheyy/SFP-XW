{
  "name": "Python 3.12 Dev Container",
  "image": "mcr.microsoft.com/devcontainers/python:3.12",
  "postCreateCommand": "apt-get update && apt-get install -y libgl1 && pip install -r requirements.txt",
  "customizations": {
    "vscode": {
      "extensions": ["ms-python.python"]
    }
  }
}

